const 
const selectOperator = (operator) ->{
    if(x !=="")
        {
            y = x
            x = ''
            operator = operator
            disableOperator();

        }
}
const enableOperator = () => {
    const operatoe_btn = document.querySelectorAll()
}